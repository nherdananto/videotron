services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      DATABASE_URL: file:/data/dev.db
    volumes:
      - videotron-db:/data
    restart: unless-stopped

volumes:
  videotron-db:
